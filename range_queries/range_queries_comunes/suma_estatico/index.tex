\subsubsection{Suma estático (prefix + diff arrays)}
    \textbf{Range sum (prefix array)}
    \lstinputlisting{\currfiledir prefix_array.cpp}

    \textbf{Range update (diff array)}
    \lstinputlisting{\currfiledir diff_array.cpp}

